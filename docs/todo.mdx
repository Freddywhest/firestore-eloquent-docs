---
sidebar_position: 5
---

# Todo

- [x] File Upload.
- [x] HTML rendered Pagination Links.
- [ ] Laravel Authentication Methods such as ~`Auth::user()`, `Auth::check()`, `Auth::id()`, `Auth::logout()`~, `Auth::loginUsingId()`, ~`Auth::attempt()`~, `Auth::viaRemember()`, `Auth::once()`, `Auth::onceBasic()`, `Auth::basic()` and `Auth::guard()`.
- [ ] Livewire Pagination.
- [ ] Any Feature you wish to add. Feel free to contribute.
- [ ] Support for core php and any other framework.
- [ ] Improve the performance of the package.